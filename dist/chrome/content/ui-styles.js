/**
 * Stream Radar — UI stylesheet (design system)
 * ------------------------------------------------------------------
 * Injected into a closed shadow root, so nothing leaks in or out.
 * Conventions:
 *   • tokens only, no magic numbers in rules
 *   • motion: 120/180/260 ms springs, always paired with prefers-reduced-motion
 *   • every interactive element is ≥ 40 px (44 px on touch) and has a visible
 *     focus ring; press feedback comes from Motion (JS) plus :active here
 *   • no emoji, no decorative glyphs: icons are Lucide SVG (src/shared/icons.js)
 * The `srad-` prefix is kept for readable DevTools output.
 */
(function (root) {
  'use strict';
  const SR = (root.SR = root.SR || {});

  SR.uiCss = `
:host { all: initial; }
*, *::before, *::after { box-sizing: border-box; }

.srad-root {
/* tokens:start - generated by tools/sync-theme.mjs, do not edit */
  --sr-canvas: #f4f5fb;
  --sr-surface: #ffffff;
  --sr-surface-2: #f3f4fa;
  --sr-surface-3: #e9ebf6;
  --sr-glass: rgba(255, 255, 255, 0.72);
  --sr-ink: #181c2b;
  --sr-ink-2: #525b75;
  --sr-ink-3: #8a92a9;
  --sr-line: #e2e5f1;
  --sr-line-2: #edeef7;
  --sr-accent: #5b50e6;
  --sr-accent-2: #8b46e8;
  --sr-accent-ink: #ffffff;
  --sr-accent-soft: #eceefd;
  --sr-accent-line: #d0d4fb;
  --sr-accent-glow: rgba(91, 80, 230, 0.38);
  --sr-ok: #0d7c55;
  --sr-ok-soft: #e6f6ee;
  --sr-warn: #8a5b00;
  --sr-warn-soft: #fdf2df;
  --sr-err: #bd2118;
  --sr-err-soft: #fcebea;
  --sr-info: #0e6088;
  --sr-info-soft: #e7f2fa;
  --sr-shadow-1: 0 1px 0 rgba(24, 28, 43, 0.02), 0 1px 2px rgba(24, 28, 43, 0.05), 0 6px 16px -8px rgba(24, 28, 43, 0.14);
  --sr-shadow-2: 0 1px 0 rgba(24, 28, 43, 0.03), 0 12px 24px -12px rgba(24, 28, 43, 0.18), 0 32px 64px -24px rgba(24, 28, 43, 0.30);
  --sr-shadow-accent: 0 8px 22px -8px var(--sr-accent-glow);
  --sr-inset: inset 0 1px 0 rgba(255, 255, 255, 0.85);
  --sr-sp-1: 4px;
  --sr-sp-2: 8px;
  --sr-sp-3: 12px;
  --sr-sp-4: 16px;
  --sr-sp-5: 22px;
  --sr-r-1: 8px;
  --sr-r-2: 12px;
  --sr-r-3: 18px;
  --sr-r-pill: 999px;
  --sr-t-fast: 120ms;
  --sr-t-mid: 190ms;
  --sr-t-slow: 280ms;
  --sr-ease: cubic-bezier(0.22, 0.72, 0.24, 1);
  --sr-spring: cubic-bezier(0.2, 0.9, 0.28, 1.24);
  --sr-font: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
  --sr-mono: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, monospace;
  --sr-brand-gradient: linear-gradient(150deg, var(--sr-accent) 0%, var(--sr-accent-2, #4338ca) 100%);
  --sr-brand-sheen: linear-gradient(180deg, rgba(255, 255, 255, 0.22), rgba(255, 255, 255, 0) 55%);
  --sr-cat-hls: linear-gradient(140deg, #fbbf24, #ea580c);
  --sr-cat-dash: linear-gradient(140deg, #38bdf8, #2563eb);
  --sr-cat-mp4: linear-gradient(140deg, #34d399, #0d9488);
  --sr-cat-webm: linear-gradient(140deg, #c084fc, #7c3aed);
  --sr-cat-blob: linear-gradient(140deg, #94a3b8, #64748b);
  --sr-cat-seg: linear-gradient(140deg, #facc15, #ca8a04);
  --sr-cat-text: linear-gradient(140deg, #2dd4bf, #0d9488);
  --sr-glyph-on-color: #ffffff;
  --sr-scroll-thumb: var(--sr-ink-2);
  --sr-scroll-thumb-hover: var(--sr-accent);
  --sr-scroll-size: 10px;
/* tokens:end */

  /* palette */

  /* geometry */

  /* motion */

  /* type */

  position: fixed !important; inset: 0 !important; z-index: 2147483000 !important;
  pointer-events: none; display: block;
  font-family: var(--sr-font); font-size: 14px; line-height: 1.45; color: var(--sr-ink);
  font-variant-numeric: tabular-nums; -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}

.srad-root[data-theme="dark"] {
/* tokens:start - generated by tools/sync-theme.mjs, do not edit */
  --sr-canvas: #0c0f1a;
  --sr-surface: #141928;
  --sr-surface-2: #1a2032;
  --sr-surface-3: #232a40;
  --sr-glass: rgba(20, 25, 40, 0.72);
  --sr-ink: #e9edf7;
  --sr-ink-2: #a1aac4;
  --sr-ink-3: #6f7a96;
  --sr-line: #28304a;
  --sr-line-2: #1e2538;
  --sr-accent: #8b93ff;
  --sr-accent-2: #b98bff;
  --sr-accent-ink: #0c0f1a;
  --sr-accent-soft: rgba(139, 147, 255, 0.16);
  --sr-accent-line: rgba(139, 147, 255, 0.40);
  --sr-accent-glow: rgba(139, 147, 255, 0.45);
  --sr-ok: #5ad39f;
  --sr-ok-soft: rgba(90, 211, 159, 0.16);
  --sr-warn: #e6b455;
  --sr-warn-soft: rgba(230, 180, 85, 0.16);
  --sr-err: #ff8a80;
  --sr-err-soft: rgba(255, 138, 128, 0.16);
  --sr-info: #7cc8f2;
  --sr-info-soft: rgba(124, 200, 242, 0.16);
  --sr-shadow-1: 0 1px 0 rgba(0, 0, 0, 0.4), 0 6px 18px -8px rgba(0, 0, 0, 0.55);
  --sr-shadow-2: 0 16px 32px -16px rgba(0, 0, 0, 0.7), 0 40px 72px -28px rgba(0, 0, 0, 0.78);
  --sr-shadow-accent: 0 10px 26px -8px var(--sr-accent-glow);
  --sr-inset: inset 0 1px 0 rgba(255, 255, 255, 0.06);
  --sr-brand-sheen: linear-gradient(180deg, rgba(255, 255, 255, 0.16), rgba(255, 255, 255, 0) 55%);
/* tokens:end */

}

/* ── floating action button ─────────────────────────────── */
.srad-fab {
  pointer-events: auto; position: absolute; right: 20px; bottom: 20px;
  width: 56px; height: 56px; border-radius: 19px; border: 1px solid var(--sr-line);
  background: var(--sr-surface); color: var(--sr-ink);
  box-shadow: var(--sr-shadow-1), var(--sr-inset);
  display: grid; place-items: center; cursor: pointer; touch-action: none;
  user-select: none; -webkit-user-select: none; -webkit-tap-highlight-color: transparent;
  transition: box-shadow var(--sr-t-mid) var(--sr-ease), background var(--sr-t-mid) var(--sr-ease), border-color var(--sr-t-mid);
  backdrop-filter: saturate(1.4) blur(20px); -webkit-backdrop-filter: saturate(1.4) blur(20px);
  isolation: isolate;
}
.srad-fab::before {
  content: ""; position: absolute; inset: 0; border-radius: inherit; opacity: 0;
  background: radial-gradient(120% 120% at 20% 0%, var(--sr-accent-soft), transparent 60%);
  transition: opacity var(--sr-t-mid) var(--sr-ease);
}
.srad-fab:hover { border-color: var(--sr-accent); box-shadow: var(--sr-shadow-2), var(--sr-inset); }
.srad-fab:hover::before { opacity: 1; }
.srad-fab:focus-visible { outline: 2px solid var(--sr-accent); outline-offset: 3px; }
.srad-fab svg { width: 24px; height: 24px; position: relative; color: var(--sr-ink); }
.srad-fab[data-live="1"] svg { color: var(--sr-accent); }
.srad-fab[data-dragging="1"] { cursor: grabbing; box-shadow: var(--sr-shadow-2); }
.srad-fab[data-dragging="1"]::after { opacity: 0; }
.srad-fab::after {
  content: ""; position: absolute; inset: -3px; border-radius: 22px; border: 1.5px solid var(--sr-accent);
  opacity: 0; pointer-events: none;
}
.srad-fab[data-pulse="1"]::after { animation: srad-ring 1.5s var(--sr-ease) 2; }
@keyframes srad-ring { 0% { opacity: .55; transform: scale(.9); } 100% { opacity: 0; transform: scale(1.28); } }
.srad-badge {
  position: absolute; top: -6px; right: -6px; min-width: 21px; height: 21px; padding: 0 6px;
  border-radius: var(--sr-r-pill); background: var(--sr-accent); color: #fff;
  font-size: 11.5px; font-weight: 700; line-height: 21px; text-align: center;
  border: 2px solid var(--sr-surface); transform: scale(0); transition: transform var(--sr-t-mid) var(--sr-spring);
}
.srad-badge[data-show="1"] { transform: scale(1); }
.srad-badge[data-empty="1"] { background: var(--sr-ink-2); }

/* ── panel shell ────────────────────────────────────────── */
.srad-panel {
  pointer-events: auto; position: absolute; right: 20px; bottom: 88px;
  width: min(444px, calc(100vw - 28px)); max-height: min(78vh, 760px);
  display: flex; flex-direction: column; overflow: hidden;
  background: var(--sr-glass); border: 1px solid var(--sr-line); border-radius: var(--sr-r-3);
  box-shadow: var(--sr-shadow-2); color: var(--sr-ink);
  backdrop-filter: saturate(1.6) blur(22px); -webkit-backdrop-filter: saturate(1.6) blur(22px);
  transform-origin: bottom right; visibility: hidden; opacity: 0;
  transition: opacity var(--sr-t-mid) var(--sr-ease), transform var(--sr-t-slow) var(--sr-spring), visibility var(--sr-t-mid);
}
.srad-panel[data-open="1"] { opacity: 1; visibility: visible; transform: none; }
.srad-panel[data-anchor="tr"] { top: 88px; bottom: auto; transform-origin: top right; }
.srad-panel[data-anchor="tl"] { right: auto; left: 20px; top: 88px; bottom: auto; transform-origin: top left; }
.srad-panel[data-anchor="bl"] { right: auto; left: 20px; bottom: 88px; transform-origin: bottom left; }

.srad-head { display: flex; align-items: center; gap: var(--sr-sp-2); padding: 10px 10px 10px 14px; border-bottom: 1px solid var(--sr-line-2); }
.srad-brand { display: flex; align-items: center; gap: 9px; min-width: 0; flex: 1 1 auto; cursor: grab; }
.srad-head[data-drag="1"] .srad-brand { cursor: grabbing; }
.srad-mark { width: 26px; height: 26px; border-radius: 8px; display: grid; place-items: center; flex: none;
  background: linear-gradient(150deg, var(--sr-accent), #3d3ac9 62%, var(--sr-ok)); color: #fff;
  box-shadow: 0 4px 14px -4px var(--sr-accent); }
.srad-mark svg { width: 15px; height: 15px; }
.srad-headtxt { min-width: 0; }
.srad-headtxt b { display: block; font-size: 13px; font-weight: 650; letter-spacing: .01em; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 210px; }
.srad-headtxt small { display: block; font-size: 11px; color: var(--sr-ink-2); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 210px; }
.srad-spacer { flex: 1 1 auto; min-width: 0; }

.srad-iconbtn {
  pointer-events: auto; position: relative; overflow: hidden; flex: none;
  width: 40px; height: 40px; display: grid; place-items: center; padding: 0;
  border: 1px solid transparent; border-radius: var(--sr-r-1); background: transparent; color: var(--sr-ink-2);
  cursor: pointer; -webkit-tap-highlight-color: transparent;
  transition: background var(--sr-t-fast) var(--sr-ease), color var(--sr-t-fast), border-color var(--sr-t-fast), transform var(--sr-t-fast);
}
.srad-iconbtn:hover { background: var(--sr-surface-2); color: var(--sr-ink); }
.srad-iconbtn:active { transform: scale(.93); }
.srad-iconbtn:focus-visible { outline: 2px solid var(--sr-accent); outline-offset: 1px; }
.srad-iconbtn svg { width: 18px; height: 18px; }
.srad-iconbtn[data-on="1"] { color: var(--sr-accent); border-color: var(--sr-line); background: var(--sr-accent-soft); }

/* ── tabs ───────────────────────────────────────────────── */
.srad-tabs { display: flex; gap: 2px; padding: 0 10px; border-bottom: 1px solid var(--sr-line-2); }
.srad-tab {
  position: relative; border: 0; background: transparent; color: var(--sr-ink-2); cursor: pointer;
  font: 600 12.5px/1 var(--sr-font); padding: 10px 11px; min-height: 38px; border-radius: 8px 8px 0 0;
  display: inline-flex; align-items: center; gap: 6px; -webkit-tap-highlight-color: transparent;
}
.srad-tab svg { width: 15px; height: 15px; }
.srad-tab:hover { color: var(--sr-ink); }
.srad-tab[aria-selected="true"] { color: var(--sr-ink); }
.srad-tab[aria-selected="true"]::after { content: ""; position: absolute; left: 10px; right: 10px; bottom: -1px; height: 2px; border-radius: 2px; background: var(--sr-accent); }
.srad-tab:focus-visible { outline: 2px solid var(--sr-accent); outline-offset: -2px; }
.srad-tab i { font-style: normal; font-size: 11px; font-weight: 700; color: var(--sr-ink-2); background: var(--sr-surface-2); border-radius: var(--sr-r-pill); padding: 1px 6px; }

/* ── meta strip ─────────────────────────────────────────── */
.srad-meta { display: flex; flex-wrap: wrap; gap: 6px; padding: 10px 14px 0; }
.srad-meta:empty { display: none; }
.srad-chip {
  display: inline-flex; align-items: center; gap: 5px; font-size: 11.5px; font-weight: 600;
  padding: 3px 9px; border-radius: var(--sr-r-pill); background: var(--sr-surface-2);
  border: 1px solid var(--sr-line-2); color: var(--sr-ink-2); max-width: 100%;
}
.srad-chip svg { width: 12px; height: 12px; }
.srad-chip[data-kind="ep"], .srad-chip[data-kind="year"] { color: var(--sr-accent); background: var(--sr-accent-soft); border-color: transparent; }
.srad-chip[data-kind="warn"] { color: var(--sr-warn); }
.srad-chip[data-kind="err"] { color: var(--sr-err); }
.srad-chip[data-kind="ok"] { color: var(--sr-ok); }

/* ── list ───────────────────────────────────────────────── */
.srad-body { flex: 1 1 auto; overflow: auto; overscroll-behavior: contain; padding: var(--sr-sp-2) 10px 4px; scrollbar-width: thin; }
.srad-body::-webkit-scrollbar { width: 10px; }
.srad-body::-webkit-scrollbar-thumb { background: var(--sr-line); border-radius: 8px; border: 3px solid transparent; background-clip: content-box; }
.srad-pane[hidden] { display: none; }

.srad-empty { text-align: center; color: var(--sr-ink-2); padding: 30px 22px 34px; }
.srad-empty svg { width: 26px; height: 26px; opacity: .55; margin-bottom: 10px; animation: srad-spin 2.4s linear infinite; }
.srad-empty b { display: block; color: var(--sr-ink); font-size: 14px; margin-bottom: 5px; }
.srad-empty p { margin: 0; font-size: 12.5px; }
@keyframes srad-spin { to { transform: rotate(360deg); } }

.srad-item {
  position: relative; display: grid; grid-template-columns: 50px minmax(0,1fr); gap: 10px;
  padding: 10px; margin-bottom: var(--sr-sp-2); border-radius: var(--sr-r-2);
  background: var(--sr-surface); border: 1px solid var(--sr-line-2); box-shadow: var(--sr-shadow-1);
  transition: border-color var(--sr-t-fast) var(--sr-ease), transform var(--sr-t-fast) var(--sr-ease), box-shadow var(--sr-t-mid);
  will-change: transform;
}
.srad-item:hover { border-color: var(--sr-accent-line); box-shadow: var(--sr-shadow-2); transform: translateY(-1px); }
.srad-item:focus-visible, .srad-item[data-active="1"] { outline: 2px solid var(--sr-accent); outline-offset: 1px; border-color: transparent; }
.srad-item[data-ad="1"] { opacity: .78; }
.srad-thumb {
  width: 50px; height: 50px; border-radius: 11px; overflow: hidden; position: relative; flex: none;
  display: grid; place-items: center; background: var(--sr-surface-2); color: #fff;
  font-size: 10px; font-weight: 800; letter-spacing: .04em;
}
.srad-thumb img { width: 100%; height: 100%; object-fit: cover; display: block; }
.srad-thumb svg { width: 20px; height: 20px; opacity: .95; }
.srad-thumb[data-cat="hls"] { background: var(--sr-cat-hls); }
.srad-thumb[data-cat="dash"] { background: var(--sr-cat-dash); }
.srad-thumb[data-cat="mp4"] { background: var(--sr-cat-mp4); }
.srad-thumb[data-cat="webm"] { background: var(--sr-cat-webm); }
.srad-thumb[data-cat="blob"] { background: var(--sr-cat-blob); }
.srad-thumb[data-cat="segment"] { background: var(--sr-cat-seg); }
.srad-thumb[data-cat="texttrack"] { background: var(--sr-cat-text); }
.srad-main { min-width: 0; }
.srad-row1 { display: flex; align-items: baseline; gap: 8px; }
.srad-name { flex: 1 1 auto; min-width: 0; font-size: 13px; font-weight: 640; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.srad-conf { flex: none; display: inline-flex; gap: 3px; align-items: center; }
.srad-conf i { width: 5px; height: 5px; border-radius: 50%; background: var(--sr-line); }
.srad-conf i[data-on="1"] { background: var(--sr-ok); }
.srad-url { margin-top: 2px; font: 10.5px/1.4 var(--sr-mono); color: var(--sr-ink-2); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.srad-tags { display: flex; flex-wrap: wrap; gap: 5px; margin-top: 7px; }
.srad-tag {
  font-size: 10.5px; font-weight: 650; padding: 2px 7px; border-radius: 7px;
  background: var(--sr-surface-2); border: 1px solid var(--sr-line-2); color: var(--sr-ink-2);
  display: inline-flex; align-items: center; gap: 4px;
}
.srad-tag svg { width: 11px; height: 11px; }
.srad-tag[data-tone="q"] { color: var(--sr-accent); background: var(--sr-accent-soft); border-color: transparent; }
.srad-tag[data-tone="ok"] { color: var(--sr-ok); }
.srad-tag[data-tone="warn"] { color: var(--sr-warn); }
.srad-tag[data-tone="err"] { color: var(--sr-err); }
.srad-tag[data-busy="1"] { color: var(--sr-accent); }
.srad-tag[data-busy="1"] svg { animation: srad-spin 1.1s linear infinite; }

.srad-actions { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 9px; }
.srad-btn {
  position: relative; overflow: hidden; pointer-events: auto; -webkit-tap-highlight-color: transparent;
  display: inline-flex; align-items: center; justify-content: center; gap: 6px;
  min-height: 34px; padding: 0 11px; border-radius: 10px; cursor: pointer;
  border: 1px solid var(--sr-line); background: transparent; color: var(--sr-ink);
  font: 620 12px/1 var(--sr-font); transition: background var(--sr-t-fast) var(--sr-ease), border-color var(--sr-t-fast), transform var(--sr-t-fast), color var(--sr-t-fast);
}
.srad-btn svg { width: 15px; height: 15px; flex: none; }
.srad-btn:hover { background: var(--sr-surface-2); border-color: var(--sr-accent); }
.srad-btn:active { transform: scale(.96); }
.srad-btn:focus-visible { outline: 2px solid var(--sr-accent); outline-offset: 2px; }
.srad-btn[data-primary="1"] { color: var(--sr-accent-ink); border-color: transparent; background: var(--sr-brand-gradient); box-shadow: var(--sr-shadow-1), var(--sr-shadow-accent); }
.srad-btn[data-primary="1"]::after { content: ""; position: absolute; inset: 0; border-radius: inherit; background: var(--sr-brand-sheen); pointer-events: none; }
.srad-btn[data-primary="1"]:hover { filter: brightness(1.06); transform: translateY(-1px); box-shadow: var(--sr-shadow-2), var(--sr-shadow-accent); }
.srad-btn[data-done="1"] { color: var(--sr-ok); border-color: var(--sr-ok); background: transparent; }
.srad-btn[disabled] { opacity: .65; cursor: default; pointer-events: none; }
.srad-btn[data-busy="1"] svg { animation: srad-spin .7s linear infinite; }
@keyframes srad-spin { to { transform: rotate(360deg); } }
/* blob rows have no shareable URL: reasoned marker instead of a party button */
.srad-no-party {
  display: inline-flex; align-items: center; gap: 6px; padding: 0 12px; min-height: 32px;
  border: 1px dashed var(--sr-line); border-radius: var(--sr-r-pill); color: var(--sr-ink-3);
  font-size: 10.5px; font-weight: 600; max-width: 100%;
}
.srad-no-party svg { width: 14px; height: 14px; flex: none; }
.srad-ripple { position: absolute; border-radius: 50%; transform: scale(0); background: currentColor; opacity: .22; pointer-events: none; }

.srad-variants { display: none; margin-top: 9px; padding-top: 8px; border-top: 1px dashed var(--sr-line); }
.srad-item[data-expanded="1"] .srad-variants { display: block; }
.srad-variant { display: flex; align-items: center; gap: 8px; font-size: 12px; color: var(--sr-ink-2); padding: 3px 2px; }
.srad-variant b { color: var(--sr-ink); font-weight: 620; }
.srad-vq { min-width: 48px; font-weight: 700; color: var(--sr-ink); }
.srad-variant .srad-btn { margin-left: auto; min-height: 32px; }
.srad-note { margin-top: 8px; font-size: 11.5px; color: var(--sr-ink-2); display: flex; gap: 6px; align-items: flex-start; }
.srad-note svg { width: 13px; height: 13px; flex: none; margin-top: 2px; }

/* ── subtitles pane ─────────────────────────────────────── */
.srad-sub-card { padding: 10px 12px; border-radius: var(--sr-r-2); background: var(--sr-surface); border: 1px solid var(--sr-line-2); margin-bottom: var(--sr-sp-2); }
.srad-sub-head { display: flex; align-items: center; gap: 8px; font-size: 12.5px; font-weight: 640; }
.srad-sub-head .srad-state { margin-left: auto; font-size: 11px; font-weight: 700; color: var(--sr-ink-2); display: inline-flex; align-items: center; gap: 5px; }
.srad-sub-head .srad-state[data-s="found"] { color: var(--sr-ok); }
.srad-sub-head .srad-state[data-s="error"], .srad-sub-head .srad-state[data-s="none"] { color: var(--sr-warn); }
.srad-sub-head .srad-state[data-s="searching"] { color: var(--sr-accent); }
.srad-sub-head .srad-state[data-s="searching"] svg { width: 12px; height: 12px; animation: srad-spin 1.1s linear infinite; }
.srad-providers { display: flex; gap: 5px; flex-wrap: wrap; margin-top: 8px; }
.srad-pv { font-size: 10.5px; padding: 2px 7px; border-radius: 7px; border: 1px solid var(--sr-line-2); color: var(--sr-ink-2); }
.srad-pv[data-s="ok"] { color: var(--sr-ok); border-color: var(--sr-ok); }
.srad-pv[data-s="error"], .srad-pv[data-s="skipped"] { color: var(--sr-warn); border-color: var(--sr-warn); }
.srad-sub-row { display: flex; align-items: center; gap: 8px; margin-top: 6px; font-size: 12px; padding: 6px 8px; border-radius: 10px; background: var(--sr-surface-2); }
.srad-sub-row span { flex: 1 1 auto; min-width: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.srad-sub-row em { font-style: normal; color: var(--sr-ink-2); font-size: 11px; }
.srad-sub-row[data-picked="1"] { outline: 1px solid var(--sr-accent); }
.srad-sub-actions { display: flex; gap: 6px; margin-top: 9px; flex-wrap: wrap; }

/* ── settings sheet ─────────────────────────────────────── */
.srad-pop {
  position: absolute; inset: 0; z-index: 3; display: flex; flex-direction: column;
  background: var(--sr-surface); color: var(--sr-ink); transform: translateY(101%);
  transition: transform var(--sr-t-slow) var(--sr-ease);
}
.srad-pop[data-open="1"] { transform: none; }
.srad-popbody { overflow: auto; padding: 4px 14px 16px; }
.srad-field { display: flex; align-items: center; gap: 10px; padding: 9px 0; border-bottom: 1px solid var(--sr-line-2); min-height: 46px; }
.srad-field > label, .srad-field > .lab { flex: 1 1 auto; font-size: 12.5px; font-weight: 600; }
.srad-field .hint { display: block; color: var(--sr-ink-2); font-weight: 400; font-size: 11.5px; margin-top: 1px; }
.srad-seg { display: inline-flex; background: var(--sr-surface-2); border: 1px solid var(--sr-line-2); border-radius: 10px; padding: 2px; gap: 2px; }
.srad-seg button { border: 0; background: transparent; color: var(--sr-ink-2); font: 600 11.5px/1 var(--sr-font); padding: 7px 10px; min-height: 32px; border-radius: 8px; cursor: pointer; -webkit-tap-highlight-color: transparent; }
.srad-seg button[data-on="1"] { background: var(--sr-accent); color: #fff; }
.srad-switch { position: relative; display: inline-flex; align-items: center; flex: none; width: 40px; height: 24px; border-radius: var(--sr-r-pill); background: var(--sr-line); border: 0; cursor: pointer; padding: 0; transition: background var(--sr-t-mid) var(--sr-ease); }
.srad-switch::after { content: ""; position: absolute; top: 3px; left: 3px; width: 18px; height: 18px; border-radius: 50%; background: #fff; box-shadow: 0 1px 3px rgba(0,0,0,.32); transition: transform var(--sr-t-mid) var(--sr-spring); }
.srad-switch[aria-checked="true"] { background: var(--sr-accent); }
.srad-switch[aria-checked="true"]::after { transform: translateX(16px); }
.srad-switch:focus-visible { outline: 2px solid var(--sr-accent); outline-offset: 2px; }

/* ── footer ─────────────────────────────────────────────── */
.srad-foot { display: flex; align-items: center; gap: 6px; padding: 8px 10px; border-top: 1px solid var(--sr-line-2); background: var(--sr-surface-2); flex-wrap: wrap; }
.srad-count { font-size: 11.5px; color: var(--sr-ink-2); font-weight: 600; }

/* ── toasts ─────────────────────────────────────────────── */
.srad-toasts { pointer-events: none; position: absolute; top: 14px; right: 14px; display: flex; flex-direction: column; gap: 8px; align-items: flex-end; width: min(360px, calc(100vw - 28px)); }
.srad-toast {
  pointer-events: auto; position: relative; overflow: hidden; display: flex; align-items: center; gap: 9px;
  max-width: 100%; padding: 10px 12px; border-radius: var(--sr-r-2);
  background: var(--sr-glass); border: 1px solid var(--sr-line); box-shadow: var(--sr-shadow-2); color: var(--sr-ink);
  backdrop-filter: saturate(1.5) blur(18px); -webkit-backdrop-filter: saturate(1.5) blur(18px);
  font-size: 12.5px; font-weight: 550;
}
.srad-toast .srad-tico { width: 22px; height: 22px; flex: none; display: grid; place-items: center; border-radius: 7px; background: var(--sr-accent-soft); color: var(--sr-accent); }
.srad-toast .srad-tico svg { width: 13px; height: 13px; }
.srad-toast[data-kind="ok"] .srad-tico { background: rgba(21,127,61,.14); color: var(--sr-ok); }
.srad-toast[data-kind="warn"] .srad-tico { background: rgba(153,97,10,.14); color: var(--sr-warn); }
.srad-toast[data-kind="err"] .srad-tico { background: rgba(180,35,24,.12); color: var(--sr-err); }
.srad-toast > span:nth-child(2) { flex: 1 1 auto; min-width: 0; }
.srad-toast button { flex: none; border: 1px solid var(--sr-line); background: transparent; color: var(--sr-ink); border-radius: 8px; font: 650 11.5px var(--sr-font); padding: 5px 8px; cursor: pointer; min-height: 32px; }
.srad-toast .srad-tbar { position: absolute; left: 0; bottom: 0; height: 2px; width: 100%; background: var(--sr-accent); transform-origin: left; opacity: .85; }

.srad-sr { position: absolute !important; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; }

/* ── responsive / touch ─────────────────────────────────── */
@media (max-width: 720px), (pointer: coarse) and (max-width: 900px) {
  .srad-fab { width: 52px; height: 52px; right: 12px; bottom: 12px; border-radius: 17px; }
  .srad-panel {
    right: 0 !important; left: 0 !important; top: auto !important; bottom: 0 !important;
    width: 100vw; max-width: 100vw; max-height: 84vh; border-radius: var(--sr-r-3) var(--sr-r-3) 0 0;
    border-bottom: 0; transform-origin: bottom center; padding-bottom: env(safe-area-inset-bottom, 0);
  }
  .srad-panel::before { content: ""; position: absolute; top: 6px; left: 50%; transform: translateX(-50%); width: 38px; height: 4px; border-radius: 2px; background: var(--sr-line); }
  .srad-iconbtn, .srad-btn, .srad-variant .srad-btn, .srad-toast button, .srad-tab { min-height: 44px; }
  .srad-btn { flex: 1 1 auto; }
  .srad-toasts { top: 8px; left: 8px; right: 8px; width: auto; align-items: stretch; }
}
@media (prefers-reduced-motion: reduce) {
  .srad-root *, .srad-root *::before, .srad-root *::after { animation-duration: 1ms !important; transition-duration: 1ms !important; }
}
@media (prefers-contrast: more) {
  .srad-root { --sr-line: currentColor; --sr-glass: ButtonFace; --sr-surface: Canvas; --sr-ink: CanvasText; }
}
`;
})(typeof globalThis !== 'undefined' ? globalThis : window);
